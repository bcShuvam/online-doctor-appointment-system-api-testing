info:
  name: Get Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{doctor}}/document-categories"
  headers:
    - name: Authorization
      value: Bearer {{admin_jwt}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
