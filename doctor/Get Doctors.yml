info:
  name: Get Doctors
  type: http
  seq: 1

http:
  method: GET
  url: "{{doctor}}/doctors?limit=10&page=1"
  headers:
    - name: Authorization
      value: Bearer {{doctor_jwt_1}}
  params:
    - name: category_id
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "10"
      type: query
    - name: page
      value: "1"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
