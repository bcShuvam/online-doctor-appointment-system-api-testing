info:
  name: Get Clinics by id
  type: http
  seq: 4

http:
  method: GET
  url: "{{doctor}}/clinics/1"
  headers:
    - name: Authorization
      value: Bearer {{doctor_jwt_1}}
    - name: Authorization
      value: Bearer {{doctor_jwt_2}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
