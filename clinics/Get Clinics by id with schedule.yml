info:
  name: Get Clinics by id with schedule
  type: http
  seq: 5

http:
  method: GET
  url: "{{doctor}}/clinics/1/full"
  headers:
    - name: Authorization
      value: Bearer {{doctor_jwt_1}}
    - name: Authorization
      value: Bearer {{doctor_jwt_2}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
