info:
  name: Send OTP
  type: http
  seq: 3

http:
  method: POST
  url: "{{doctor}}/auth/send-otp"
  body:
    type: json
    data: |-
      {
        "email": "bcshuvam@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
