info:
  name: login Patient
  type: http
  seq: 1

http:
  method: POST
  url: "{{doctor}}/auth/login"
  body:
    type: json
    data: |-
      {
        "identifier": "shuvambc@gmail.com",
        "password": "shuvambc@12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
