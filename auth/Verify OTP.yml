info:
  name: Verify OTP
  type: http
  seq: 4

http:
  method: POST
  url: "{{doctor}}/auth/verify-otp"
  body:
    type: json
    data: |-
      {
        "email": "bcshuvam@gmail.com",
        "otp": 449278
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
