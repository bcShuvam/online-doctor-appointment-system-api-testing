info:
  name: Verify email
  type: http
  seq: 2

http:
  method: POST
  url: "{{doctor}}/auth/resend-verification"
  body:
    type: json
    data: |-
      {
        "email": "bcshuvam@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
