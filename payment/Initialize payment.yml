info:
  name: Initialize payment
  type: http
  seq: 1

http:
  method: POST
  url: "{{doctor}}/payment/initialize-khalti-payment"
  body:
    type: json
    data: |-
      {
        "item_id": 1,
        "total_price": 110, 
        "website_url": "http://localhost:8000"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
