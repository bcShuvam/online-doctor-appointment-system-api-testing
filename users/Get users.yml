info:
  name: Get users
  type: http
  seq: 2

http:
  method: GET
  url: "{{doctor}}/users"
  headers:
    - name: Authorization
      value: Bearer {{admin_jwt}}
  params:
    - name: limit
      value: "10"
      type: query
      disabled: true
    - name: page
      value: "2"
      type: query
      disabled: true
    - name: search
      value: radha
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
