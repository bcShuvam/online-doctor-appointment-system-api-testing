info:
  name: Delete users
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{doctor}}/patients/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
