info:
  name: Delete users
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{doctor}}/users/1"
  headers:
    - name: jwt
      value: Bearer {{jwt}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
