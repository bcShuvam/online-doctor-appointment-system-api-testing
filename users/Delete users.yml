info:
  name: Delete users
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{doctor}}/users/5"
  headers:
    - name: Authorization
      value: Bearer {{admin_jwt}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
