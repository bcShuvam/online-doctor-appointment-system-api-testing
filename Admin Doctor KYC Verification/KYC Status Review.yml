info:
  name: KYC Status Review
  type: http
  seq: 2

http:
  method: PUT
  url: "{{doctor}}/admin-doctor-kyc/3/review"
  headers:
    - name: Authorization
      value: Bearer {{admin_jwt}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
