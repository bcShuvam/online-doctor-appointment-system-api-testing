info:
  name: View ALL KYC
  type: http
  seq: 1

http:
  method: GET
  url: "{{doctor}}/admin-doctor-kyc"
  headers:
    - name: Authorization
      value: Bearer {{admin_jwt}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
