info:
  name: KYC Status Reject
  type: http
  seq: 4

http:
  method: PUT
  url: "{{doctor}}/admin-doctor-kyc/3/reject"
  headers:
    - name: Authorization
      value: Bearer {{admin_jwt}}
  body:
    type: json
    data: |-
      {
        "reason": "Fake documents"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
