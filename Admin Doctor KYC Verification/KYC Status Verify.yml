info:
  name: KYC Status Verify
  type: http
  seq: 3

http:
  method: PUT
  url: "{{doctor}}/admin-doctor-kyc/3/verify"
  headers:
    - name: Authorization
      value: Bearer {{admin_jwt}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
