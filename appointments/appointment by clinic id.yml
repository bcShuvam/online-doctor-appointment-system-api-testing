info:
  name: appointment by clinic id
  type: http
  seq: 5

http:
  method: GET
  url: "{{doctor}}/appointments/clinic/1"
  headers:
    - name: Authorization
      value: Bearer {{patient_jwt_1}}
      disabled: true
    - name: Authorization
      value: Bearer {{recptionist_jwt_1}}
    - name: Authorization
      value: Bearer {{doctor_jwt_1}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "reason": "Doctor not available at selected time"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
