info:
  name: GET Receptionists
  type: http
  seq: 1

http:
  method: GET
  url: "{{doctor}}/receptionists"
  headers:
    - name: Authorization
      value: Bearer {{admin_jwt}}
  params:
    - name: search
      value: nita
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
